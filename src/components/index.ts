/**
 * VITAL AI — Shared component barrel.
 *
 * Re-exports reusable presentational components as they are built. Layout:
 *   components/ui/       primitives (Button, Card, Input, Chip, Badge)
 *   components/charts/   data visualisation (RingProgress, BarChart, LineChart)
 *   components/layout/   structure (Screen, Header, Section)
 *
 * Feature-specific components live with their screen, not here.
 */

export {};
